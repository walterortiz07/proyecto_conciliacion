# Ejecucion local del servicio (la base PostgreSQL es compartida; sus datos
# de conexion se toman del archivo .env). En el VPS, Coolify usa el Dockerfile.
services:
  servicio-conciliador:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
